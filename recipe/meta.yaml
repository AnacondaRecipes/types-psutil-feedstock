{% set name = "types-psutil" %}
{% set version = "7.0.0.20251116" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name.replace('-', '_')}}-{{ version }}.tar.gz
  sha256: 92b5c78962e55ce1ed7b0189901a4409ece36ab9fd50c3029cca7e681c606c8a

build:
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vv
  skip: true  # [py<39]

requirements:
  host:
    - python
    - pip
    - setuptools >=77.0.3
  run:
    - python

test:
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/python/typeshed
  summary: Typing stubs for psutil
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  description: |
    This is a type stub package for the psutil package.
    It can be used by type checkers to check code that uses psutil.
    This version of types-psutil aims to provide accurate annotations for psutil==7.0.*.
  doc_url: https://github.com/python/typeshed/blob/main/README.md
  dev_url: https://github.com/python/typeshed

extra:
  recipe-maintainers:
    - mr-c
  skip-lints:
    - missing_imports_or_run_test_py
